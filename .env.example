# Database - Supabase 연결 정보 (실제 값은 Render 환경 변수에서 설정)
DATABASE_URL=postgresql://user:password@host:port/database

# API Settings
API_V1_STR=/api/v1
PROJECT_NAME=Cosmetic Recommendation API
DEBUG=False

# Server
HOST=0.0.0.0
PORT=8000

# Production Settings
ENVIRONMENT=production